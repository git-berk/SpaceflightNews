// Model types are deeply immutable (all vals, never mutated after construction),
// but Compose infers List<String> as unstable. Declaring them here keeps
// :core:model free of a Compose dependency while still letting composables skip.
com.berco.spaceflightnews.core.model.*
