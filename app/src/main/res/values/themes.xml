<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all app theming; this only styles the window before setContent runs. -->
    <style name="Theme.SpaceflightNews" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
    </style>

    <style name="Theme.SpaceflightNews.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/app_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.SpaceflightNews</item>
    </style>
</resources>
